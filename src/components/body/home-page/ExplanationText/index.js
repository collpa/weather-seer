import "./styles.scss";

const ExplanationText = () => {
  return (
    <p>
      Enter the name of a city then click on the button below <br />
      and find out what is the weather expected for tomorrow!
    </p>
  );
};

export default ExplanationText;
